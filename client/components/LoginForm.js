import React from 'react';

const LoginForm = React.createClass({
    render() {
        return (
            <div>
                Formulario de login!
            </div>
        )
    }
});

export default LoginForm;
